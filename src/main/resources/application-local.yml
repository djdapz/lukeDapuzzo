spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lukedapuzzo
    password:
    username: devondapuzzo
    driver-class-name: org.postgresql.Driver

flyway:
  locations: classpath:db/migration,classpath:db/fixtures
  schemas: public
  clean-on-validation-error: true