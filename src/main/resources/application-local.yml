spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lukedapuzzo?currentSchema=local
    password:
    username: devondapuzzo
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        default_schema: local

dapuzzo:
  luke:
    client:
      url: http://localhost:9000/bundle.js

flyway:
  schemas: local
