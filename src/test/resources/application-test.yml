spring:
  datasource:
    url: ${CONCOURSE_POSTGRES_URL:jdbc:postgresql://localhost:5432/lukedapuzzo}
    password: ${CONCOURSE_POSTGRES_PASSWORD:devondapuzzo}
    username: ${CONCOURSE_POSTGRES_USER:devondapuzzo}
    driver-class-name: org.postgresql.Driver

server:
  port: ${PORT}


dapuzzo:
  luke:
    client:
      url: http://localhost:9000/bundle.js
    email-distribution-list: devon@dapuzzo.com,djdapz@aol.com