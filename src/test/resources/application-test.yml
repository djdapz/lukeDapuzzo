spring:
  datasource:
    url: ${LUKE_DB_URL:jdbc:postgresql://localhost:5432/lukedapuzzo}
    password: ${LUKE_DB_PASSWORD:devondapuzzo}
    username: ${LUKE_DB_USERNAME:devondapuzzo}
    driver-class-name: org.postgresql.Driver

server:
  port: ${PORT}


dapuzzo:
  luke:
    client:
      url: http://localhost:9000/bundle.js
    email-distribution-list: devon@dapuzzo.com,djdapz@aol.com