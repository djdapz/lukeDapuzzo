---
platform: linux

image_resource:
  type: docker-image
  source: {repository: openjdk, tag: 8-jdk-alpine}

inputs:
- name: luke-dapuzzo-app

#run:
#  path: luke-dapuzzo-app/pipeline/run-tests
#  path: pwd

run:
  path: cd
  args: ['luke-dapuzzo-app/pipeline && ls -lsa && sh run-tests.sh']
